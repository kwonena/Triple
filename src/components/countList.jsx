import React, { memo } from "react";
import styled from "styled-components";

const CountList = memo((props) => {
  return <h1>hi</h1>;
});

const LogoImg = styled.img`
  width: 400px;
  height: 338px;
`;

export default CountList;
